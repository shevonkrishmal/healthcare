<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthcare">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Doctor_profile_PV"></activity>
        <activity android:name=".Doctor_channelingDetails_edit"  />
        <activity android:name=".Doctor_appoinments" />
        <activity android:name=".Doctor_channelingDetails" />
        <activity android:name=".Doctor_EditProfile" />
        <activity android:name=".Doctor_Profile" />
        <activity android:name=".Doctor_list"/>
        <activity android:name=".Doctor_Login" />
        <activity android:name=".Doctor_Register" />
        <activity android:name=".MainActivity" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>